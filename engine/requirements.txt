# NumPy 2 note: a clean install (CI / fresh venv) resolves consistently against
# numpy 2.x — sklearn/pandas pull numpy-2 wheels and their optional binary
# accelerators (pyarrow, bottleneck, numexpr) are imported only if present.
# A *conda base* env can still fail collection with "AttributeError: _ARRAY_API
# not found" when it carries older accelerators compiled against numpy 1.x.
# Fix without downgrading numpy:  pip install -U pyarrow bottleneck numexpr
# (or run the engine in a dedicated venv:  python -m venv .venv && pip install -r engine/requirements.txt)
fastapi==0.115.6
uvicorn[standard]==0.34.0
scikit-learn==1.6.1
joblib==1.4.2
pandas==2.2.3
pydantic==2.10.4
numpy==2.2.1
httpx==0.28.1
pytest==8.3.4
# Tier 2 (Phase 4): biometric LSTM + ensemble + SHAP
torch==2.13.0
shap==0.51.0
xgboost==2.1.4
